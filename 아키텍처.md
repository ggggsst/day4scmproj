> 한줄 요약: Next.js 기반의 월간 발주계획 업무 플로우와 Supabase 분석 화면을 결합한 한국후지필름BI 조달관리 MVP입니다.

# 프로젝트 아키텍처

> 작성 기준일: 2026-08-27  
> 기준 위치: 저장소 루트 `C:\Users\kimta\SCM Project\superSCM`  
> 대상: 기기·옵션 월간 발주계획 MVP

## 1. 시스템 개요

이 프로젝트는 한국후지필름BI의 월간 발주계획 업무를 웹 화면으로 보여주는 Next.js 15 + React 19 + TypeScript 프로토타입이다. 현재 루트 프로젝트는 두 가지 흐름을 함께 가진다.

1. `/` 경로: 클라이언트 상태만 사용하는 Phase 1 업무 플로우 프로토타입
2. `/analysis/leadtime` 경로: Supabase `analytics.v_leadtime_gap`를 읽는 Phase 2 분석 예제

전체 구조는 다음과 같다.

```text
브라우저
  └─ Next.js App Router
      ├─ app/page.tsx
      │   └─ components/procurement-app.tsx
      │       └─ components/workflow/*
      └─ app/analysis/leadtime/page.tsx
          ├─ lib/scm.ts
          │   └─ lib/supabase/server.ts
          ├─ lib/scm-model.ts
          └─ components/analysis/*

Supabase PostgreSQL
  ├─ raw       원본 데이터
  ├─ core      정제·매핑·확정 기준
  └─ analytics 화면 조회용 뷰
```

화면 계산을 담당하는 공식 계산 엔진은 아직 완성된 상태가 아니다. 루트의 업무 플로우는 대표 샘플값과 브라우저 상태를 사용하며, Supabase 분석 화면은 DB 뷰에서 이미 계산된 결과를 조회한다.

## 2. 기술 스택과 실행 구조

| 항목 | 사용 기술 및 역할 |
|---|---|
| 애플리케이션 | Next.js 15 App Router |
| UI | React 19, TypeScript |
| 스타일 | `app/globals.css`의 전역 순수 CSS |
| 아이콘 | `lucide-react` |
| 데이터베이스 | Supabase PostgreSQL |
| 데이터 접근 | `@supabase/supabase-js`, `@supabase/ssr` |
| 테스트 | Node 내장 테스트 러너(`node:test`) |
| 배포 설정 | Vercel(`vercel.json`) |
| 경로 별칭 | `@/*` → 프로젝트 루트(`tsconfig.json`) |

### 요청 흐름

- `/` 요청은 `app/page.tsx`가 `ProcurementApp`을 렌더링한다. `ProcurementApp`은 `useState`로 현재 업무 단계를 관리하고, 각 단계 컴포넌트를 선택한다.
- `/analysis/leadtime` 요청은 서버 컴포넌트인 `page.tsx`가 `getLeadtimeGap()`을 호출한다. 조회 결과는 `normalizeLeadtimeGap()`으로 화면 모델로 바뀐 뒤 `DataTable`에 전달된다.
- Supabase 조회 오류와 빈 결과는 구분한다. 오류는 실패 메시지를 표시하고, 빈 결과는 표의 빈 상태 메시지를 표시한다.

## 3. 폴더별 구조와 파일 역할

### `app/` — Next.js 라우팅 및 전역 화면

App Router의 라우트, 레이아웃, 전역 스타일을 담당한다.

| 파일 | 역할 |
|---|---|
| `app/layout.tsx` | 전역 HTML 레이아웃. 한국어 문서 언어, 페이지 제목·설명, `globals.css`를 설정한다. |
| `app/page.tsx` | `/` 루트 진입점. `components/procurement-app.tsx`를 렌더링한다. |
| `app/globals.css` | 앱 전체 색상 토큰, 레이아웃, 카드, 폼, 표, 상태 배지, 반응형 스타일을 정의한다. 별도 Tailwind·CSS Module은 사용하지 않는다. |
| `app/analysis/leadtime/page.tsx` | 공급처별 마스터 리드타임과 실제 P80의 격차를 보여주는 서버 분석 화면. `dynamic = 'force-dynamic'`으로 최신 조회를 사용한다. |
| `app/api/health/supabase/route.ts` | `GET /api/health/supabase` API. Supabase 환경변수 설정 여부를 확인한다. DB 연결 자체를 검증하는 엔드포인트는 아니다. |

### `components/` — 재사용 가능한 화면 컴포넌트

#### `components/procurement-app.tsx`

전체 업무 플로우의 클라이언트 셸이다. 여섯 단계(`전체 현황 → 수요 확정 → 재고·공급 → 마스터 검증 → 발주량 계산 → 보고자료`)를 정의하고, 사이드바·상단바·진행 표시·단계 이동을 관리한다.

#### `components/workflow/` — Phase 1 업무 단계 화면

| 파일 | 역할 |
|---|---|
| `dashboard-step.tsx` | 전체 현황 대시보드. KPI와 업무 단계 카드를 보여주고 각 단계로 이동시킨다. |
| `demand-step.tsx` | OL, SFDC, Bulk-deal, 과거 실적 Trend, 수급회의 탭을 대표 데이터로 보여주며 수요 상태와 회의 결과를 브라우저 상태로 편집한다. |
| `supply-step.tsx` | 전월말 재고와 Open PO의 가용성·위험 상태를 대표값으로 보여준다. |
| `master-step.tsx` | 품목, BOM, 장착율·사용량, MOQ, Lead Time, Flexibility Rule 준비 여부를 체크리스트 형태로 보여준다. |
| `calculation-step.tsx` | 발주량 계산 결과, 예외, MOQ·Flexibility Rule 적용 결과를 대표값으로 보여주는 화면이다. |
| `report-step.tsx` | 총 발주금액, 전월·전년·OL 대비 비교 및 Excel/PDF 출력 예정 상태를 보여준다. |
| `step-frame.tsx` | 모든 업무 단계 하단의 이전/다음 이동 버튼과 프로토타입 안내 문구를 제공한다. |

#### `components/analysis/` — Supabase 분석 화면 공통 UI

| 파일 | 역할 |
|---|---|
| `analysis-frame.tsx` | 분석 화면의 제목·설명·`SUPABASE LIVE` 배지를 감싸는 공통 프레임이다. |
| `data-table.tsx` | 제네릭 컬럼 정의를 받아 분석 데이터를 표로 렌더링한다. 정렬 방향, 셀 렌더러, 행 키, 빈 결과 문구를 지원한다. `formatNumber()`도 제공한다. |

### `lib/` — 도메인 모델, 조회, Supabase 클라이언트

화면이 DB 접근이나 정규화 로직을 직접 갖지 않도록 경계를 둔 계층이다.

| 파일 | 역할 |
|---|---|
| `lib/scm-model.ts` | 화면 모델 타입 `LeadtimeGap`과 컬럼명 후보를 이용한 `normalizeLeadtimeGap()`을 정의한다. DB 뷰의 영문·한글·구버전 컬럼명 차이를 흡수한다. |
| `lib/scm-model.test.ts` | 리드타임 행 정규화 테스트. 영문 별칭, 한글 별칭, 실제 `analytics.v_leadtime_gap` 컬럼명을 검증한다. |
| `lib/scm.ts` | Supabase 조회 함수 모음. 현재 `getLeadtimeGap()`과 `getStockoutKpi()`를 제공하며, 분석 화면은 이 파일을 통해 DB를 읽는다. |
| `lib/supabase.ts` | 브라우저·서버 Supabase 클라이언트와 환경변수 함수의 공개 진입점이다. |
| `lib/supabase/client.ts` | 브라우저용 Supabase 클라이언트를 만든다. publishable key만 사용한다. |
| `lib/supabase/server.ts` | 서버용 Supabase 클라이언트를 만든다. 세션 유지·자동 토큰 갱신을 끈 읽기 중심 설정이다. |
| `lib/supabase/env.ts` | `NEXT_PUBLIC_SUPABASE_URL`과 `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`를 읽고, 누락 시 한국어 오류를 발생시킨다. secret key는 다루지 않는다. |

### `supabase/` — Supabase 로컬 설정과 마이그레이션

| 파일 | 역할 |
|---|---|
| `supabase/config.toml` | Supabase CLI 로컬 프로젝트 ID, API 활성화, PostgreSQL 15 설정을 정의한다. |
| `supabase/.temp/cli-latest` | Supabase CLI가 생성한 로컬 임시 버전 정보이다. 애플리케이션 로직에는 관여하지 않는다. |
| `supabase/migrations/20260813000100_create_procurement_demand_core.sql` | 초기 수요확정 스키마를 만든다. `planning_runs`, `ol_demand`, `sfdc_pipeline`, `bulk_deals`, `historical_actuals`, `demand_confirmations` 테이블, 인덱스, `updated_at` 트리거를 정의한다. 현재 마이그레이션은 `public` 스키마를 대상으로 한다. |

### `sql/` — 운영 DB 권한·보안 보조 SQL

| 파일 | 역할 |
|---|---|
| `sql/01-grants.sql` | `core`·`analytics` 스키마 사용 권한과 조회 권한을 `anon`, `authenticated`에 부여한다. `raw`는 의도적으로 공개하지 않는다. |
| `sql/02-policies.sql` | 수업용으로 `core.leadtime_plan`, `core.usage_profile`에 읽기·쓰기 권한과 전체 허용 RLS 정책을 추가한다. 운영 환경에서는 권한을 좁혀야 한다. |

### `docs/` — 실습·설계 문서

| 경로 | 역할 |
|---|---|
| `docs/04-실습안내.md` | 4회차 Supabase 분석 실습의 준비 절차, 확인 건수, 오전·오후 산출물을 안내한다. |
| `docs/superpowers/04-실습안내.md` | 같은 실습을 개인 Supabase DB 구축과 단계별 SQL 실행 관점에서 더 자세히 안내한다. |
| `docs/superpowers/plans/2026-08-13-procurement-planning-mvp-plan.md` | Phase 1 프로토타입 구현 계획과 작업별 파일·검증 항목을 기록한다. |
| `docs/superpowers/specs/2026-08-13-procurement-planning-mvp-prd.md` | MVP 요구사항, 사용자 흐름, 비목표, 향후 확장 방향을 보관하는 설계 사본이다. |

### `outputs/019ff8b7-725b-7b41-99a2-f3b7bc66ee76/` — 생성된 업무 산출물

프로젝트 구현 코드가 아니라 업무 프로세스 정의와 미리보기 결과를 보관한다.

| 파일 종류 | 역할 |
|---|---|
| `기기_옵션_월간발주_프로세스정의서.xlsx` | 사용 안내, 프로세스맵, 상세 프로세스, 계산 규칙, 데이터 정의, RACI, KPI, 발주계산 템플릿, 구축 입력사항, 샘플자료 체크, 정책결정표, FX-LIVE 연계 정의를 담은 원본 workbook이다. |
| `*.xlsx.inspect.ndjson` | workbook 검사·구조 확인 결과를 저장한 NDJSON이다. |
| `preview_00_사용안내.png` ~ `preview_11_FXLIVE연계정의.png` | 각 workbook 시트를 이미지로 미리보기한 파일이다. |

### 루트의 설정·문서·데이터 파일

| 파일 | 역할 |
|---|---|
| `package.json` | 프로젝트 의존성 및 `dev`, `build`, `start`, `test` 스크립트를 정의한다. |
| `package-lock.json` | npm 의존성의 재현 가능한 버전을 고정한다. |
| `tsconfig.json` | 엄격한 TypeScript 컴파일, `@/*` 경로 별칭, Next.js 타입 포함 범위를 설정한다. |
| `next.config.ts` | Next.js 설정. 현재 React Strict Mode를 활성화한다. |
| `next-env.d.ts` | Next.js가 생성·관리하는 타입 선언 진입 파일이다. |
| `vercel.json` | Vercel에서 Next.js 프레임워크로 배포하도록 선언한다. |
| `README.md` | 설치·실행 방법, 현재 Phase 1 범위, 향후 구현 항목, Supabase 연결 절차를 설명한다. |
| `README_배포전_확인.md` | 배포 전 확인 항목을 정리한 운영 문서이다. |
| `AGENTS.md` | 프로젝트 작업 규칙, 데이터 접근 원칙, CSS·한국어·검증 규칙을 정의한다. |
| `SCHEMA.md` | `raw`, `core`, `analytics`의 역할과 주요 테이블·뷰 컬럼 및 기대 건수를 정의한다. |
| `2026-08-13-procurement-planning-mvp-prd.md` | 제품 목표, 업무 범위, 데이터 입력, 계산, 보고서 출력에 대한 원본 PRD이다. |
| `적용방법.md` | 4회차 준비 파일 복사, Supabase 설정, 권한 부여, 점검 방법을 안내한다. |
| `dump.sql` | Supabase/PostgreSQL 원본 데이터 및 스키마를 복원하기 위한 대용량 덤프 파일이다. 앱 코드가 직접 읽지는 않는다. |
| `.env`, `.env.local` | 로컬 환경변수 파일이다. Supabase URL·publishable key를 보관하며 커밋하면 안 된다. |
| `.gitignore` | 환경변수, 빌드 결과, 의존성 등 Git 제외 대상을 정의한다. |

### 루트의 데이터·문서 생성 스크립트

| 파일 | 역할 |
|---|---|
| `build_dummy_demand_data.mjs` | `@oai/artifact-tool`로 OL, SFDC, Bulk-deal, 실적 Trend, 수급회의, 월별 요약 더미 workbook을 생성한다. 현재 파일 안에 특정 로컬 절대 출력 경로가 있어 실행 전 확인이 필요하다. |
| `build_workbook.mjs` | 업무 프로세스 정의서 workbook과 시트별 서식을 생성한다. 마찬가지로 특정 로컬 절대 출력 경로를 사용한다. |

## 4. 데이터 아키텍처

`SCHEMA.md` 기준 데이터 계층은 아래와 같다.

| 계층 | 역할 | 애플리케이션 사용 원칙 |
|---|---|---|
| `raw` | CSV 원본 적재 영역 | 화면에서 직접 조회·수정하지 않는다. |
| `core` | 공급처 별칭, 확정 리드타임, 사용 프로파일 등 정제·기준 영역 | 회사 기준과 매핑을 저장한다. 일부 테이블은 수업 중 확정값을 저장한다. |
| `analytics` | 리드타임, 소진 위험, KPI, 사용 프로파일 등 화면용 뷰 | 화면과 AI가 원칙적으로 조회하는 영역이다. |

주요 분석 조회 경로는 다음과 같다.

```text
analytics.v_leadtime_gap
  → lib/scm.ts:getLeadtimeGap()
  → lib/scm-model.ts:normalizeLeadtimeGap()
  → app/analysis/leadtime/page.tsx
  → components/analysis/data-table.tsx
```

현재 코드의 Supabase 마이그레이션은 `public` 수요확정 테이블을 만들고, 분석 조회 코드는 `analytics` 스키마를 사용한다. 따라서 두 데이터 모델은 목적이 다르며, 향후 실제 저장 기능을 연결할 때 `public` 수요확정 모델과 `core`·`analytics` 분석 모델의 관계를 명확히 정리해야 한다.

## 5. 현재 구조의 주의점과 확장 지점

- 루트 `/` 업무 화면은 아직 DB에 저장하지 않는다. `demand-step.tsx`의 수요 상태·회의 상태는 브라우저 메모리에만 존재한다.
- 화면 컴포넌트에서 평균·분위수 같은 분석 계산을 하지 않고, 분석 결과는 DB 뷰에서 받아야 한다. 새 분석 화면은 `lib/scm-model.ts` → `lib/scm.ts` → `app/analysis/<기능>/page.tsx` → `components/analysis/*` 순서를 따른다.
- `getStockoutKpi()`는 조회 함수만 준비되어 있고, 현재 루트 라우트에는 소진 위험 화면이 없다. `docs/04-실습안내.md`의 `/analysis/stockout`은 예정 범위다.
- `sql/02-policies.sql`의 전체 허용 정책은 수업용이다. 운영 배포 전에는 인증 사용자와 역할별 권한으로 변경해야 한다.
- `raw`를 직접 노출하지 않도록 `core`·`analytics`만 API Exposed schemas에 등록하고, Supabase 조회에는 `.schema('analytics')` 또는 필요한 스키마를 명시해야 한다.
- `app/api/health/supabase/route.ts`는 환경변수 존재 여부만 확인하므로, 실제 권한·스키마 노출·뷰 조회 가능 여부까지 확인하려면 별도 DB 점검이 필요하다.

## 6. 중복 디렉터리 `superSCM/`에 대한 기록

루트 바로 아래의 `superSCM/`는 루트 프로젝트와 거의 동일한 파일을 가진 별도 복사본이며, 현재 Git 상태에서 통째로 미추적(`?? superSCM/`)이다. 실행·버전 관리 기준은 현재 작업 루트인 바깥쪽 프로젝트로 판단했다.

복사본에는 루트에 없는 다음 파일이 추가되어 있다.

| 파일 | 역할 |
|---|---|
| `superSCM/app/analysis/layout.tsx` | 분석 라우트 전용 레이아웃으로 추정되는 파일이다. |
| `superSCM/components/analysis/analysis-tabs.tsx` | 분석 화면 간 탭 이동을 제공하는 확장 컴포넌트다. |
| `superSCM/.env.example`, `superSCM/.env.local.example` | Supabase 환경변수 템플릿이다. |
| `superSCM/README_배포전_확인.md`, `superSCM/적용방법.md` | 루트 문서의 복사본 또는 갱신본이다. |

복사본을 실제 개발 대상으로 사용할 계획이라면 먼저 어느 디렉터리를 정식 저장소로 삼을지 결정하고, 중복 디렉터리를 병합·정리해야 한다. 두 디렉터리를 동시에 수정하면 동일한 화면의 버전이 갈라질 수 있다.

현재 루트에서 `npm run build`를 실행하면 `tsconfig.json`의 전체 파일 포함 범위 때문에 이 미추적 복사본도 함께 타입 검사된다. 그 결과 `superSCM/app/analysis/layout.tsx`가 루트 기준 `@/components/analysis/analysis-tabs`를 찾지 못해 빌드가 실패한다. 복사본을 제거·병합하거나, 정식 프로젝트 범위에서 제외하거나, 복사본의 내부 경로를 일관되게 정리해야 한다.

## 7. 권장 변경 순서

새 분석 기능을 추가할 때는 다음 순서를 지킨다.

1. `lib/scm-model.ts`에 타입과 컬럼 정규화 함수를 추가한다.
2. `lib/scm.ts`에 `analytics` 뷰 조회 함수를 추가한다.
3. `app/analysis/<기능이름>/page.tsx`에 서버 화면을 만든다.
4. 필요하면 `components/analysis/`의 공통 프레임·표를 재사용한다.
5. 정규화 테스트를 `lib/scm-model.test.ts`에 추가한다.
6. 변경 후 `npm test`와 `npm run build`로 검증한다.
